const Events = () => {
  const containerStyle = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh',
    backgroundColor: '#f0f0f0',
    fontFamily: 'Arial, sans-serif',
  };

  const messageStyle = {
    fontSize: '24px',
    color: '#333',
  };

  return (
    <div style={containerStyle}>
      <div style={messageStyle}>Events page is yet to be updated.</div>
    </div>
  );
};

export default Events;